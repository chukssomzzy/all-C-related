# include <stdio.h> 

  int main(){
     
      double floatVar = 50.0f;   
      printf("%f", floatVar);  //implicit promotion
                               //implicit promotion is when a type is converted implicit to match the type requirement of a calling function 
     
      return 0;
  }
